---
date: 2026-08-23
user: Martin
category: preference
tags: [card, border, outline, footer]
---

# Card outline is a boolean; no footer separator

`Card` variants (`default`, `elevated`, `muted`) are all borderless. Use the `outline` boolean for a strong border. `CardFooter` has no top border by default (`separator` defaults to `false`).
